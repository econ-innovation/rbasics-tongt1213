##如何将3.141592657 转换成 character
as.character(3.141592657)

#如何将3.141592657 转换成 logical
as.logical(3.1415926)

#如何判断"hello world"是否是一个character
is.character("hello world")

## 如何判断一个NA是否是缺失值
is.na(NA)

v1 <- c(1,2,3,4)
v2 <- c(2,3,4,5)
v3 <- c(v1,v2)
v4 <- c(1,2,'c')

#取出v1的第2和第3个元素

v1[c(2, 3)]


#取出v1的最后???个元素

v1[length(v1)]

#取出v1的倒数第???个元素

v1[length(v1) - 1]

#取出v1中整除3的元素

v1[v1 %% 3 == 0]

#v1[-1]会返回什么结果，有何启示？
v1[-1]
#返回 c(2, 3, 4)
#负数索引用于排除某些元素

#v1[5]会返回什么结果，有何启示？

#v1[5] 会返回 NA
#使用超出向量长度的索引时，R会返回缺失值

# v1[] \<- 99 与 v1 \<- 99 有何不同？

## v1[] <- 99 会将v1中的所有元素替换为99
## v1 <- 99 会将99赋值给v1